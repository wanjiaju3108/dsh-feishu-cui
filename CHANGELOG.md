# 更新日志

这个插件每一版改了什么。版本号按 `package.json` 里的版本走。

## 0.2.0

### 适配新版 DSH（要求 0.1.7-rc.1 及以上）

- 四项配置（当前会话、绑定的 user、当前工作区、防休眠开关）改用新版设置机制；0.1.5 那代写不进去。

### 会话名恢复显示

- 设置页的会话下拉、飞书「会话列表」重新显示会话名；无名时才退回 ID。

### 会话没了明说

- 当前会话已不存在时回「当前会话已经不在了，重新打开会话列表选一个」，不再回「这条消息没能交给会话，稍后再试」。

### 设置卡结果另发一张卡

- 模型 / 推理深度 / 权限 / 会话重命名四张卡点「确定」后停在「已请求」；成功另发「…已修改」，失败另发失败原因。
- 会话名被改会收到「会话名已修改」；模型自动起的标题不通知。

### 插电合盖时不休眠（只有 macOS）

- 设置页多一个开关「插电合盖时不休眠」：打开后，插件跑着期间一直按住系统休眠；插电才生效，拔电照旧。
- 只有 macOS 有这个开关，别的平台上它是灰的。

### 升级

- 旧配置自动导入，不用手工搬。
